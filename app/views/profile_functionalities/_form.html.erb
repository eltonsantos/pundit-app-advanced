<%= form_for(@profile_functionality) do |f| %>
  <% if @profile_functionality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_functionality.errors.count, "error") %> prohibited this profile_functionality from being saved:</h2>

      <ul>
      <% @profile_functionality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :profile_id %><br>
    <%= f.text_field :profile_id %>
  </div>
  <div class="field">
    <%= f.label :functionality_id %><br>
    <%= f.text_field :functionality_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
